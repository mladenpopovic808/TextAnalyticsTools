import { TokenNullPipePipe } from './token-null-pipe.pipe';

describe('TokenNullPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TokenNullPipePipe();
    expect(pipe).toBeTruthy();
  });
});
